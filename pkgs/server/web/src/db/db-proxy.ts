export const getDbProxy = async (
  mode: 'dev' | 'prod' | 'pkg',
  dbname: string
) => {
  return {} as any
}

export const getDbsProxy = async (mode: 'dev' | 'prod' | 'pkg') => {
  return {} as any
}
